class Vector
{
	__init__(x, y)
	{
		self.x = x;
		self.y = y;
	}

	print()
	{
		print(str(self.x) + ", " + str(self.y));
	}
}


func main()
{
	var arr = Array();
	arr.push(Vector(1, 2));
	arr.push(Vector(3, 4));
	arr.push(Vector(5, 6));

	for(var i = 0; i < arr.size(); i+=1)
	{
		arr.get(i).print();
		print("\n");
	}
}

func println(s)
{
	print(str(s) + "\n");
}
