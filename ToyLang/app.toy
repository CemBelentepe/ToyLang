func main()
{
	var t0 = clock();
	println(fibb(20));
	print("Time elapsed: ");
	print(clock() - t0);
	print(" sec\n");
}

func fibb(x)
{
	if (x <= 1)
		return 1;
	else 
		return fibb(x-1) + fibb(x-2);
}

func println(s)
{
	print(s);
	print("\n");
}
