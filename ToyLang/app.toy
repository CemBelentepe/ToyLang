func main()
{
	var arr = Array();
	for(var i = 0; i < 10; i+=1)
		arr.push(i);
	arr.pop();
	arr.set(2, 20);
	for(var i = 0; i < arr.size(); i+=1)
		print(str(arr.get(i)) + " ");
}

func println(s)
{
	print(str(s) + "\n");
}
