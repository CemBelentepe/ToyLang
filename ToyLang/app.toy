func main()
{
	var t0 = clock();
	while(true)
	{
		var t1 = clock();
		var dt = t1 - t0;
		if(dt > 0.5)
		{
			println(t1);
			t0 = t1;
		}
	}
}

func fibb(x)
{
	if (x <= 1)
		return 1;
	else 
		return fibb(x-1) + fibb(x-2);
}

func println(s)
{
	print(s);
	print("\n");
}
